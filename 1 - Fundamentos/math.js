const raio = 5.6;

/**
 * O Object Math possui classes para operações matemáticas,
 * a constantes PI e a operação de exponenciação
 */
const area = Math.PI * Math.pow(raio, 2);

console.log((area).toFixed(2));
console.log(typeof Math);